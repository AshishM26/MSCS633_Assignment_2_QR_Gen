# Application output screenshot

`application_output.png` is the original, unaltered screenshot supplied by the student on September 20, 2026. It shows the running Tkinter application with the repository URL, generated QR code, success message, and saved output path.

The screenshot is embedded under section 3 of `report/MSCS633_Hands_On_Assignment_2_Report.docx` and referenced in `report/report_text.md` and the project README.

To replace the screenshot after a future application change, generate the repository URL in the app and capture the complete window using macOS Shift–Command–4 followed by Space, or the screenshot tool on your operating system. Update both the image file and the embedded picture in Word before committing.
